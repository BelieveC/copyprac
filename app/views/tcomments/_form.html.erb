<%= form_for(@tcomment) do |f| %>
  <% if @tcomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tcomment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @tcomment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :thesis_id %><br>
    <%= f.text_field :thesis_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

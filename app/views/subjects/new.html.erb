<div id="page_wrapper">

	<% if @subject.errors.any?%>
		<div id="errors">
			<h2><%= pluralize(@subject.errors.count, "Error")%> prevented this subject from saving</h2>
			<ul>
				<% @subject.errors.full_messages.each do |msg| %>
					<li><%= msg%></li>
				<% end%>
			</ul>
		</div>
	<% end %>
	<h4 id="head"> New Subject</h4>
	<%=render('form')%>
	<br>
	<%=link_to "Back",:back,class:"button"%>
</div>

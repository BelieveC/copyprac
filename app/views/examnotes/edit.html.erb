<div id="page_wrapper">

	<% if @examnote.errors.any?%>
		<div id="errors">
			<h2><%= pluralize(@examnote.errors.count, "Error")%> prevented this examnote from saving</h2>
			<ul>
				<% @examnote.errors.full_messages.each do |msg| %>
					<li><%= msg%></li>
				<% end%>	
			</ul>
		</div>
	<% end %>
	<h4 id="head"> Edit Examnote</h4>
	<%=render('form')%>
	<br>
	<%=link_to "Back",:back,class:"button"%>
</div>
